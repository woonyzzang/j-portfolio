/**
 * objectKeys
 * @description 객체의 키값으로 데이터 순회
 * @param {Object} obj - 객체 데이터
 * @returns {Object} 객체 데이터의 키값을 반환
 */
const objectKeys = (obj: object): object => Object.keys(obj);

export {
    objectKeys
};
